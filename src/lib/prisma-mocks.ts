export enum LayoutPreset {
  STACK_VERTICAL = "STACK_VERTICAL",
  GRID = "GRID",
  BENTO = "BENTO",
  HORIZONTAL_SCROLL = "HORIZONTAL_SCROLL",
  CAROUSEL_2D = "CAROUSEL_2D"
}

export enum Plan {
  FREE = "FREE",
  PRO = "PRO"
}

export enum ButtonStyle {
  ROUNDED = "ROUNDED",
  SOFT = "SOFT",
  SHARP = "SHARP",
  GHOST = "GHOST"
}

export type Profile = any;
export type PublicPage = any;
export type LinkItem = any;
